# Copy this file to .env and fill in the values.
# Flask auto-loads .env when python-dotenv is installed.

# Flask secret key used for session signing (optional — a default is hardcoded in config.py)
SECRET_KEY=change-me

# MongoDB connection string (set automatically by docker-compose; override here for local use)
MONGO_URI=mongodb://localhost:27017/NOU_Enrollment
